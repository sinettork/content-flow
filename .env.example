# Use mock for the self-contained demo and supabase in deployed environments.
VITE_DATA_BACKEND=mock

# Browser-safe credentials only. Never expose a secret/service-role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
